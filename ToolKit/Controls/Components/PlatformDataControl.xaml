<UserControl x:Class="mapKnight.ToolKit.Controls.Components.PlatformDataControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Margin="20">
        <TextBlock>Waypoints</TextBlock>
        <TextBlock x:Name="textblock_count" >
            <Run Text="Count: "/>
            <Run Text="{Binding Count, Mode=OneWay}"/>
        </TextBlock>
        <ListView x:Name="listbox_waypoints"/>
        <Button HorizontalAlignment="Center" Click="button_reset_Click">Reset</Button>
    </StackPanel>
</UserControl>
